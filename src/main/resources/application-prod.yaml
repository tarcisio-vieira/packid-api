spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/packid}
    username: ${DB_USER:packid_user}
    password: ${DB_PASSWORD:!@JM12337050vsg@jkhg-mltjmj!#2080}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    org.springframework.web: INFO
